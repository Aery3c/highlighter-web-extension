import { addMark, removeMark, updatePopperState, removeTab, addTab, updateTab } from './reducers/tabs';
import { updatePrimaryColor, updateTheme, toggleTheme } from './reducers/config';

export {
  addMark,
  removeMark,
  removeTab,
  updateTab,
  addTab,
  updatePopperState,
  updatePrimaryColor,
  updateTheme,
  toggleTheme
}